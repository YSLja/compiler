# How to Run

**Mac:**
```
./run.sh
```

**Windows:**
```
run.bat
```

To test a specific file, pass it as an argument:
```
./run.sh myfile.g
run.bat myfile.g
```

If no file is given, it runs `test.g` by default.
